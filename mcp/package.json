{
  "name": "quern-debug-mcp",
  "version": "0.9.0",
  "description": "MCP server for Quern Debug Server — thin wrapper over HTTP API",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "quern-debug-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 dist/index.js",
    "dev": "tsc --watch"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  }
}
